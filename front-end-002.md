## 호이스팅 (Hoisting) 과 클로저(Closure)

### 1. 호이스팅

- 자바 스크립트 고유의 특징
- 변수 / 선언문이 해당 스코프의 최상단으로 끌어 올려진 것처럼 동작
- var 선언은 undefined 로 초기화
    - 선언과 동시에 undefined로 초기화가 함께 이루어지므로, 선언 전 접근에도 undefined 출력
- let, const 선언은 TDZ 에 머물러 접근이 제한
    - ReferenceError 를 발생시킴
    - 선언만 먼저 되고, 코드의 실행 흐름이 실제 선언문 위치에 도달할 때까지 초기화가 유예
    - 유예 되는 동안을 TDZ에 머무른다고 표현

```
**스코프?**
		- 변수와 함수가 접근 할 수 있는 유효 범위
		- 현재 스코프 -> 상위 스코프 -> 전역 스코프 순으로 식별자 접근 검색
		
		
**TDZ?**
		- Temporal Dead Zone : 일시적 사멸 구역
		- let 또는 const 로 선언 된 변수가 선언 및 초기화 되기 전까지 접근할 수 없는 구간
		- 변수가 선언되기 전 사용되는 예측 불가 코드 흐름 방지
		- const 일관성 보장 : 상수 값인 const의 고유 특성 유지
						=> 상수 값은 선언과 동시에 값 할당 및 재할당이 불가함
```

---

### 2. 클로저

- 내부 함수가 외부 함수의 변수를 기억하고 접근 할 수 있는 상태
- 외부 함수의 실행이 종료되어 메모리에서 사라져도, 내부 함수가 실행 중이면 참조하는 변수는 계속 유지
- 정보 은닉 , 상태 유지의 장점
    - private 접근 제한자 처럼 접근에 대해 제한을 둘 수 있음
        - 특정 함수를 통해서만 변수 조작 가능
    - 함수가 호출 될 때마다 초기화 되는 것이 아닌, 이전 상태를 기억하고 이어감
    - 예시
    
    ```jsx
    function 외부함수() {
    	let count = 0; 
    		// => 지역 변수 : 외부함수 종료시 원래 사라지고, 호출시마다 초기화 되어야 함
    		
    	return function() {
    		// => 내부 함수로, count 를 실 사용할 함수를 리턴
    		count++;
    		return count
    			// => 카운트를 1씩 더해서 리턴 해주는 함수
    	}
    }
    	// => 외부 함수는 카운트 변수를 선언하고 내부 함수를 리턴 해주는 함수
    	
    	const counter = 외부함수();
    		// => 외부 함수를 호출하여 변수에 저장 (현재 외부함수는 내부 함수만을 리턴)
    	
    		counter(); // --> count = 1
    		// => 리턴한 함수(내부 함수)를 실행 
    			// -> 외부 함수는 종료 되었지만, 외부 함수를 그대로 참조하여 count 값 증가
    	counter(); // --> count = 2
    	counter(); // --> count = 3
    		...
    		// => 내부 함수가 클로저로, 계속 count 값을 기억하며 사용
    ```
