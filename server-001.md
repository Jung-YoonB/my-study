## 1. WS 와 WAS 의 차이점

### 1-1. WS

- Web Server
- 정적 자원을 응답 해 주는 서버
- 정적 자원 종류
    - html
    - css
    - image 등

### 1-2. WAS

- Web Application Server
- 내부에 서블릿 컨테이너를 가지고 있음
    
    ```
    
    **서블릿 컨테이너?**
    
    - Tomcat 등 서블릿의 생성, 실행, 소멸 등 생명주기를 관리하는 주체
    - 싱글톤 관리
        - 최초 1회만 생성되어 모든 클라이언트 요청에서 재사용 되는 관리 방식
    - JSP 변환
        - 내부적으로 .jsp 파일에 작성 된 HTML 과 자바코드를 HttpServlet 을 상속 받은
        서블릿 클래스로 변환 하여 실행
    ```
    
- 정적 자원 뿐만 아닌 DB 조회, 비즈니스 로직 실행과 같은 동적 처리도 가능한 서버

---

## 2. Spring 의 주요 특징

### 2-1. DI

- 의존성 주입 (Dependency Injection)
- 필요한 **객체를 직접 생성하지 않고** 외부(스프링 컨테이너)에서 **주입 받아 사용**하는 방식
- 코드의 유연성과 재사용성 향상
- 주입 방법 3가지 (@AutoWired 사용)
    - 필드 주입
    - **생성자 주입 (권장)**
        - AutoWired 생략 가능
    - setter 주입

### 2-2. IoC

- 제어의 역전 (Inversion of Control)
- 객체의 생성과 생명 주기 **관리를 개발자가 아닌 스프링 컨테이너가 담당**하는 설계 원칙
    
    ```
    
    **스프링 컨테이너?**
    
    - 자바 객체(Bean) 들의 생명 주기를 관리하고 생성부터 소멸까지 책임지는 핵심 공간
        - 주요 역할
            - Bean 생성 및 등록
            - DI → 의존 관계 주입
            - 생명 주기 관리
        - 대표적 종류
            - BeanFactory
                - 최상위 인터페이스
                - Bean 생성, 조회
            - ApplicationContext
                - BeanFactory 를 상속 받은 확장 컨테이너
                - 메시지 소스(다국어 처리)
                - 이벤트 발행
                - 환경 변수 처리 등 애플리케이션 구동 시 필요한 부가 기능 제공
    ```
    
- 어노테이션을 클래스 상단에 정의 하여 Spring Bean 영역에 등록 후 관리하도록 지정
    - @Component
    - @Controller
    - @RestController
    - @ResponseBody
    - @RequestMapping("경로")
    - @Service
    - @Repository 등

### 2-3. POJO

- Plain Old Java Object
- 스프링 어노테이션 등 특정 프레임 워크 기술에 종속되지 않은 **순수한 자바 객체**
- 객체 지향적 설계 가능
- 특정한 컨테이너 없이도 단위 테스트 수행 가능
    
    ```
    
    **단위 테스트?**
    
    - 소프트웨어의 가장 작은 단위가 의도대로 정확히 동작하는지 독립적으로 검증 테스트
        - 하나의 클래스나 메소드 등을 테스트
    - 특징과 장점
        - 격리성
        - 빠른 피드백
        - 유지보수 용이
   ```
    

### 2-4. AOP

- 관점 지향 프로그래밍 (Aspect-Oriented Programming)
- 여러 곳에서 **공통으로 사용하는 기능을 비즈니스 로직과 분리하여 관리**하는 프로그래밍 방식
    - 로깅, 보안, 트랜잭션 관리 등
- 코드의 중복을 줄이고 유지 보수성 향상
---
